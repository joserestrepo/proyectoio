import { PrimercaractermayusculaPipe } from './primercaractermayuscula.pipe';

describe('PrimercaractermayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimercaractermayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
